import 'package:flutter/material.dart';

class FindToiletPage extends StatelessWidget {
  const FindToiletPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Find Toilet'),
      ),
      body: const Center(
        child: Text('Find Toilet Page - Coming Soon'),
      ),
    );
  }
} 